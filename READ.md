1. Each implementation is located under their respective directories
2. Compile and run(javac && java) PerformanceTester<acronym>.java to run the implementations with the predefined parameters
3. Note: For the HyperLogLog set implementation(Reffered to as Probabilistic set), it is located under the my-app folder. As the HLL package is required, maven is used to include the dependency.